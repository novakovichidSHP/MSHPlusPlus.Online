# Техническое руководство

## Обзор
МШПайтон.Онлайн — это статическое одностраничное приложение (SPA), которое запускает Python через Skulpt и предоставляет редактор файлов, консоль ввода/вывода и холст для графики Turtle. Основной поток обрабатывает интерфейс, хранилище и отрисовку Turtle. Все данные остаются в браузере пользователя.

## Документация кода
Основные файлы содержат JSDoc комментарии для ключевых функций. Для работы с исходным кодом:
- **[assets/skulpt-app.js](../assets/skulpt-app.js)** — основная логика UI и управление проектами

Для генерации полной HTML документации выполните:
```bash
npm install --save-dev jsdoc
npx jsdoc -c jsdoc.json
```

Документация будет доступна в `docs/api/` после выполнения.

В CI документация API обновляется автоматически на `push`:
- workflow генерирует `docs/api` через JSDoc;
- выполняется проверка покрытия ключевых runtime-методов;
- при изменениях `docs/api` изменения коммитятся ботом в ту же ветку.

## Тесты
Unit-тесты запускаются встроенным runner Node.js:
```bash
npm run test:unit
```

Тесты лежат в `tests/unit/`.

Покрытие (coverage):
```bash
npm run test:unit:coverage
```

## Структура каталогов
- `index.html`: Оболочка SPA Skulpt и CSP.
- `assets/skulpt-app.js`: Логика интерфейса и выполнения (роутер, редактор, хранилище, шеринг).
- `assets/skulpt-styles.css`: Стили и верстка интерфейса.
- `assets/skulpt-fflate.esm.js`: Резервный вариант gzip для браузеров без CompressionStream.
- `assets/vendor/skulpt/`: Среда выполнения Skulpt и стандартная библиотека.
- `docs/`: Документация и руководства.

## Процесс загрузки
1. `index.html` загружает `assets/skulpt-app.js` как модуль.
2. `init()` привязывает обработчики интерфейса, запускает анимацию landing и инициализирует состояние редактора.
3. Открывается IndexedDB; при блокировке используется резервное хранилище в памяти.
4. Инициализируется runtime Skulpt и обработчики роутинга (`hashchange` + первый `router()`).
5. Роутер открывает проект/снимок/embed и выполняет первичный рендер.

## Роутинг
Используется роутинг на основе хеша (поддерживается GitHub Pages):
- `/#/` : главная страница.
- `/#/p/{projectId}` : редактируемый проект.
- `/#/s/{shareId}?p={payload}` : неизменяемый снимок.
- `/#/embed` : режим встраивания с настройками в строке запроса.

## Адаптивная компоновка IDE
В IDE используются два брейкпоинта:
- `@media (max-width: 1024px)` — tablet-режим.
- `@media (max-width: 768px)` — mobile-режим.

### Tablet (`<=1024px`)
- Приоритет по площади у трёх основных компонентов: редактор, консоль и черепаха.
- Панель модулей остаётся доступной в компактной колонке.
- Кнопка `Горячие клавиши` скрывается.
- В поле ввода консоли используется подсказка: `Введите input и нажмите «Отправить»`.

### Mobile (`<=768px`)
- Включается карточный режим: одновременно отображается один крупный блок (`modules/editor/console/turtle`).
- Переключение осуществляется через нижнюю панель `#mobile-nav`.
- По умолчанию открывается карточка `editor`.
- После запуска:
  - если в коде используется turtle, активируется карточка `turtle`;
  - иначе активируется карточка `console`;
  - при ожидании `input()` приоритет у карточки `console`.
- При открытии экранной клавиатуры нижняя навигация временно скрывается (`body.keyboard-open`), чтобы уменьшить перекрытия.

### Размеры и доступность
- Для мобильных кнопок используется touch-friendly целевой размер (ориентир `44px` по высоте).
- Для панели модулей на планшете добавлен перенос текста в кнопках без выхода за пределы контейнера.

## Модель хранения
База данных IndexedDB: `mshp-ide-skulpt`

Хранилища объектов:
- `projects`: `{ projectId, title, files, assets, lastActiveFile, updatedAt }`
- `blobs`: `{ blobId, data }`
- `drafts`: `{ key, overlayFiles, deletedFiles, draftLastActiveFile, updatedAt }`
- `recent`: `{ key: "recent", list: [projectId...] }`
- `trash`: `{ key: "trash", list: [projectId...] }`

## Горячие клавиши

### Обработка в основном потоке
Горячие клавиши регистрируются в функции `init()` через слушатели событий `keydown`:

**Глобальные горячие клавиши (document level):**
- F8 или Alt+R → `runActiveFile()`
- Alt+X → `stopExecution()`
- Alt+C → `clearConsole()`
- Alt+1/2/3 → переключение фокуса между редактором, консолью и холстом

**Горячие клавиши редактора (onEditorKeydown):**
- Tab → добавление отступа
- Alt+/ → комментирование строки
- Alt+↑/↓ → перемещение строки
- Ctrl+D → дублирование строки
- Ctrl+Shift+K → удаление строки
- Ctrl+L → выделение строки

**Горячие клавиши консоли:**
- Enter (без Shift) → отправка многострочного ввода как отдельных input() вызовов
- Shift+Enter → добавление новой строки в поле ввода

### Отображение справки
Функция `showHotkeysModal()` отображает полный список горячих клавиш в модальном диалоговом окне.

Если IndexedDB недоступна, используется хранилище в оперативной памяти на базе `Map`.

## Выполнение кода
- Выполнение Python происходит в основном потоке через API Skulpt.
- Перед запуском применяется нормализация исходника, настройка `Sk.configure` и подготовка turtle-окружения.
- Точка входа активного файла запускается как `__main__`.
- Остановка выполнения делается через `Sk.execLimit` и runtime-таймаут.

## Конвейер Turtle
- `TURTLE_SHIM` подменяет стандартный модуль `turtle` внутри runtime Skulpt.
- Отрисовка выполняется в основном потоке на canvas с управлением скоростью и анимацией.
- События указателя/касания нормализуются на стороне UI и передаются turtle-слою.

## Шеринг и снимки
- При шеринге состояние проекта сериализуется в JSON и кодируется как UTF-8 байты.
- Сжатие данных:
  - Используется `CompressionStream("gzip")`, если доступно.
  - Резервный вариант — `gzipSync` из `fflate`.
- Кодирование: base64url с префиксом:
  - `g.<data>` для gzip.
  - `u.<data>` для обычного текста.
- `shareId`:
  - SHA-256 через `crypto.subtle`, если доступно.
  - Резервный вариант — хеш на основе FNV-1a.
- Снимки неизменяемы; локальные правки сохраняются в `drafts`.

## CSP и COI
- CSP (мета-тег в `index.html`) включает:
  - `script-src 'self' 'unsafe-eval'`
  - `worker-src 'self' blob:`
- В репозитории не используется service worker для COI-заголовков; если нужны COOP/COEP/CORP, их следует выставлять на уровне хостинга.

## Совместимость с браузерами
Резервные механизмы в `assets/skulpt-app.js`:
- Полифиллы для TextEncoder/TextDecoder.
- Генерация UUID без `crypto.randomUUID`.
- `CompressionStream/DecompressionStream` -> `fflate` gzip.
- `crypto.subtle digest` -> FNV-1a.
- `navigator.clipboard` -> `document.execCommand`.
- IndexedDB -> хранилище `Map` в памяти.
- Pointer events -> обработчики мыши/касаний.

Поддерживаемые браузеры:
- Chrome / Chromium
- Firefox
- Safari

Неподдерживаемые окружения:
- устаревшие браузеры без WebAssembly, Web Workers и современного ES-модульного окружения.

## Лимиты и защитные механизмы
- `RUN_TIMEOUT_MS`: 60 секунд.
- `MAX_OUTPUT_BYTES`: 2,000,000 байт.
- `MAX_FILES`: 30.
- `MAX_SINGLE_FILE_BYTES`: 50,000 байт.
- `MAX_TOTAL_TEXT_BYTES`: 250,000 байт.
- Основной runtime не требует этапа сборки; отдельный шаг сборки нужен только для пересборки локального CM6 bundle (`npm run build:cm6`).

## Известные ограничения
- **⚠️ DEPRECATED**: Загрузка файлов через UI (Assets) больше не поддерживается. Экспорт и импорт проектов работают нормально.
- Снимки не зашифрованы; они закодированы и опционально сжаты.
- Внешние сетевые запросы заблокированы политикой CSP.
